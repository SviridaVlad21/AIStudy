# AI Agent - Kotlin Multiplatform Project

Полноценный Kotlin Multiplatform проект с AI-агентом для Android и iOS.

## 📋 Описание

Этот проект реализует простого AI-агента с использованием Kotlin Multiplatform (KMP). Агент может отвечать на вопросы пользователя, используя OpenAI API (или любой другой совместимый LLM API).

## 🏗️ Архитектура

### Shared Module (общий код)
```
shared/src/commonMain/kotlin/com/example/aistudy/
├── agent/
│   └── AiAgent.kt           # Основной класс AI-агента
├── repository/
│   └── AiRepository.kt      # HTTP-клиент для работы с API
├── model/
│   ├── Message.kt           # Модели сообщений
│   └── ApiModels.kt         # Модели для API запросов/ответов
└── Example.kt               # Примеры использования
```

### Android App (MVVM архитектура)
```
composeApp/src/
├── commonMain/kotlin/com/example/aistudy/
│   ├── viewmodel/
│   │   └── ChatViewModel.kt # ViewModel для управления состоянием чата
│   └── ui/
│       └── ChatScreen.kt    # UI компоненты (Compose Multiplatform)
└── androidMain/kotlin/com/example/aistudy/
    ├── MainActivity.kt      # Главная Activity
    └── App.kt              # Точка входа Compose
```

### iOS App (SwiftUI)
```
iosApp/iosApp/
├── iOSApp.swift            # Точка входа приложения
└── ContentView.swift       # SwiftUI чат интерфейс
```

## 🚀 Технологии

- **Kotlin Multiplatform** - кроссплатформенная разработка
- **Ktor Client** - HTTP-клиент для сетевых запросов
- **kotlinx.serialization** - сериализация JSON
- **Jetpack Compose / Compose Multiplatform** - UI для Android
- **SwiftUI** - UI для iOS
- **MVVM** - архитектурный паттерн для Android

## 📦 Зависимости

Все зависимости настроены в `gradle/libs.versions.toml`:

```toml
[versions]
ktor = "3.0.3"
kotlinx-serialization = "1.8.0"
kotlinx-coroutines = "1.10.1"

[libraries]
# Ktor
ktor-client-core
ktor-client-content-negotiation
ktor-client-serialization
ktor-client-logging
ktor-client-darwin (iOS)
ktor-client-okhttp (Android)

# Kotlinx
kotlinx-serialization-json
kotlinx-coroutines-core
```

## 🔧 Настройка

### 1. Получение API ключа

Для работы с AI вам нужен API ключ от OpenAI:
1. Зарегистрируйтесь на https://platform.openai.com/
2. Создайте API ключ в разделе API Keys
3. Скопируйте ключ

### 2. Добавление API ключа в проект

**ВАЖНО:** Не коммитьте API ключ в репозиторий!

Замените `"YOUR_API_KEY_HERE"` на ваш реальный API ключ в следующих файлах:

**Для Android:**
```kotlin
// composeApp/src/commonMain/kotlin/com/example/aistudy/viewmodel/ChatViewModel.kt
class ChatViewModel : ViewModel() {
    private val aiAgent = AiAgent(apiKey = "sk-your-actual-api-key")
    // ...
}
```

**Для iOS:**
```swift
// iosApp/iosApp/ContentView.swift
let agent = AiAgent(apiKey: "sk-your-actual-api-key")
```

**Рекомендация:** В production используйте переменные окружения или безопасное хранилище.

## 🎯 Использование AiAgent

### Базовое использование

```kotlin
import com.example.aistudy.agent.AiAgent
import kotlinx.coroutines.runBlocking

suspend fun main() {
    val agent = AiAgent(apiKey = "YOUR_API_KEY_HERE")

    try {
        // Простой запрос
        val response = agent.ask("Что такое Kotlin Multiplatform?")
        println("Ответ: $response")
    } catch (e: Exception) {
        println("Ошибка: ${e.message}")
    } finally {
        agent.close()
    }
}
```

### Безопасное использование с Result

```kotlin
suspend fun askQuestion() {
    val agent = AiAgent(apiKey = "YOUR_API_KEY_HERE")

    val result = agent.askSafe("Объясни преимущества KMP")

    result.onSuccess { answer ->
        println("Успешный ответ: $answer")
    }.onFailure { error ->
        println("Ошибка: ${error.message}")
    }

    agent.close()
}
```

### Функция ask()

```kotlin
/**
 * @param question Вопрос пользователя
 * @return Ответ от AI в виде строки
 * @throws Exception если произошла ошибка
 */
suspend fun ask(question: String): String
```

### Функция askSafe()

```kotlin
/**
 * Безопасная версия, возвращает Result вместо выброса исключения
 * @param question Вопрос пользователя
 * @return Result<String> с ответом или ошибкой
 */
suspend fun askSafe(question: String): Result<String>
```

## 🏃 Запуск проекта

### Android

```bash
# Из корня проекта
./gradlew :composeApp:installDebug

# Или откройте проект в Android Studio и запустите конфигурацию composeApp
```

### iOS

```bash
# Открыть в Xcode
open iosApp/iosApp.xcodeproj

# Или из командной строки
xcodebuild -project iosApp/iosApp.xcodeproj -scheme iosApp -sdk iphonesimulator
```

**Примечание:** Для iOS требуется macOS с установленным Xcode.

## 📱 Интерфейс приложения

### Android (Jetpack Compose)
- **Список сообщений** - ScrollView с автопрокруткой к новым сообщениям
- **Поле ввода** - TextField с поддержкой многострочного ввода
- **Кнопка отправки** - Активна только когда есть текст
- **Индикатор загрузки** - Показывает процесс обработки запроса
- **Обработка ошибок** - Snackbar с сообщением об ошибке

### iOS (SwiftUI)
- **Список сообщений** - ScrollView с LazyVStack
- **Поле ввода** - TextField с автоматической высотой
- **Кнопка отправки** - Иконка отправки с визуальной индикацией
- **Индикатор загрузки** - ProgressView с текстом
- **Обработка ошибок** - Alert с сообщением об ошибке

## 🔄 Обработка ошибок

Проект включает базовую обработку следующих ошибок:

1. **Пустой вопрос** - `IllegalArgumentException`
2. **Сетевые ошибки** - "Ошибка сети. Проверьте подключение к интернету."
3. **Таймаут** - "Превышено время ожидания. Проверьте подключение к интернету."
4. **API ошибки** - Сообщения от сервера OpenAI
5. **Неизвестные ошибки** - Общее сообщение с описанием

## 📂 Структура файлов

```
AIStudy/
├── gradle/
│   └── libs.versions.toml          # Версии зависимостей
├── shared/                         # Общий модуль
│   ├── build.gradle.kts
│   └── src/
│       ├── commonMain/
│       ├── androidMain/
│       └── iosMain/
├── composeApp/                     # Android приложение
│   ├── build.gradle.kts
│   └── src/
│       ├── commonMain/             # Общий UI код
│       └── androidMain/            # Android специфичный код
├── iosApp/                         # iOS приложение
│   └── iosApp/
│       ├── iOSApp.swift
│       └── ContentView.swift
├── settings.gradle.kts
├── build.gradle.kts
└── gradle.properties
```

## 🌐 Смена API провайдера

Код легко адаптируется под другие API (Ollama, Claude, LocalAI и др.):

1. Измените `baseUrl` в `AiRepository.kt`:
```kotlin
class AiRepository(
    private val apiKey: String = "",
    private val baseUrl: String = "http://localhost:11434/v1" // Ollama
)
```

2. Обновите модели запросов/ответов в `ApiModels.kt` если требуется.

## ⚙️ Требования

- **JDK**: 11 или выше
- **Kotlin**: 2.2.20
- **Android Studio**: Arctic Fox или новее
- **Xcode**: 14.0 или новее (только для iOS)
- **macOS**: Только для сборки iOS
- **Gradle**: 8.0 или выше

## 🧪 Пример запроса (консольный)

Запустите пример из `shared/src/commonMain/kotlin/com/example/aistudy/Example.kt`:

```kotlin
suspend fun main() {
    val agent = AiAgent(apiKey = "YOUR_API_KEY_HERE")

    try {
        val response = agent.ask("Что такое Kotlin Multiplatform?")
        println("Ответ: $response")
    } finally {
        agent.close()
    }
}
```

## 📝 Заметки

- **API ключи**: Используйте переменные окружения или секретное хранилище в продакшене
- **Timeout**: По умолчанию установлен на 60 секунд
- **Logging**: Ktor клиент логирует все запросы (можно отключить в `AiRepository.kt`)
- **Coroutines**: Все сетевые вызовы асинхронные

## 🎓 Обучающие примеры

См. файл `shared/src/commonMain/kotlin/com/example/aistudy/Example.kt` для дополнительных примеров:
- Одиночный вопрос
- Несколько вопросов подряд
- Обработка ошибок

## 📄 Лицензия

Этот проект создан в образовательных целях.

## 🤝 Вклад

Проект готов к расширению:
- Добавьте сохранение истории чатов
- Реализуйте стриминг ответов
- Добавьте поддержку изображений
- Создайте систему промптов
- Добавьте локальную базу данных

---

**Готово к запуску и тестированию!** 🚀