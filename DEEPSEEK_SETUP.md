# 🚀 Настройка DeepSeek API - Быстрый старт

## Что такое DeepSeek?

**DeepSeek** - это мощная китайская языковая модель, которая:
- ⚡ **Быстрая** - отличная скорость ответа
- 💰 **Дешевая** - значительно дешевле GPT-4
- 🎯 **Качественная** - конкурирует с GPT-3.5/4
- 🔧 **OpenAI-совместимая** - использует тот же API формат

## 📝 Шаг 1: Получите API ключ DeepSeek

### 1.1 Регистрация

1. Перейдите на https://platform.deepseek.com
2. Нажмите "Sign Up" или "注册" (регистрация)
3. Зарегистрируйтесь через email или GitHub

### 1.2 Получение API ключа

1. После входа перейдите в раздел **API Keys** или **API密钥**
2. Нажмите "Create API Key" или "创建API密钥"
3. Скопируйте ключ (начинается с `sk-`)
4. **ВАЖНО**: Сохраните ключ в безопасном месте!

### 1.3 Пополнение баланса (опционально)

DeepSeek может предоставлять бесплатные кредиты для тестирования.
Для production использования пополните баланс через:
- Alipay (支付宝)
- WeChat Pay (微信支付)
- Карты (в зависимости от доступности)

## 🔐 Шаг 2: Настройка API ключа в проекте

### Вариант 1: local.properties (Рекомендуется)

1. **Скопируйте template:**
```bash
cp local.properties.template local.properties
```

2. **Откройте `local.properties`** и вставьте ваш ключ:
```properties
deepseek.api.key=sk-ваш-реальный-ключ-здесь
```

3. **Готово!** Файл `local.properties` уже в `.gitignore` и не будет закоммичен.

### Вариант 2: Прямая установка в код (НЕ рекомендуется для production)

Для быстрого тестирования можете временно установить ключ напрямую:

**Android/Kotlin:**
```kotlin
// В файле с вашей точкой входа (MainActivity.kt или Application.kt)
import com.example.aistudy.config.ApiConfig

class MainActivity : ComponentActivity() {
    override fun onCreate(savedInstanceState: Bundle?) {
        super.onCreate(savedInstanceState)

        // Установка API ключа
        ApiConfig.initialize("sk-ваш-ключ-здесь")

        setContent {
            App()
        }
    }
}
```

**iOS:**
```swift
// В файле iOSApp.swift
import Shared

@main
struct iOSApp: App {
    init() {
        // Установка API ключа
        ApiConfig.shared.initialize(key: "sk-ваш-ключ-здесь")
    }

    var body: some Scene {
        WindowGroup {
            ContentView()
        }
    }
}
```

## ✅ Шаг 3: Проверка настройки

### Android

```bash
./gradlew :composeApp:installDebug
```

Откройте приложение и отправьте тестовый вопрос: "Привет!"

### iOS

```bash
open iosApp/iosApp.xcodeproj
# В Xcode нажмите Run (⌘R)
```

Откройте приложение и отправьте тестовый вопрос: "Привет!"

## 🎯 Преимущества текущей архитектуры

### Централизованное хранение

Весь код использует `ApiConfig` - единую точку конфигурации:

```kotlin
// shared/src/commonMain/kotlin/com/example/aistudy/config/ApiConfig.kt
object ApiConfig {
    const val BASE_URL = "https://api.deepseek.com"
    const val MODEL = "deepseek-chat"
    const val TEMPERATURE = 0.7
    const val MAX_TOKENS = 2048
    var apiKey: String = ""  // Устанавливается один раз при старте
}
```

**Преимущества:**
- ✅ Ключ храниться в одном месте
- ✅ Легко изменить модель или параметры
- ✅ Безопасно (через `local.properties`)
- ✅ Работает на всех платформах

### Автоматическая загрузка

При старте приложения ключ загружается автоматически:

```kotlin
// Android - ChatViewModel
init {
    val apiKey = ApiKeyProvider.getApiKey()  // Из local.properties
    if (apiKey.isNotEmpty()) {
        ApiConfig.initialize(apiKey)
    }
}
```

## 🔄 Переключение между провайдерами

### Вернуться на OpenAI

Измените в `ApiConfig.kt`:
```kotlin
const val BASE_URL = "https://api.openai.com"
const val MODEL = "gpt-3.5-turbo"
```

И замените ключ в `local.properties`:
```properties
deepseek.api.key=sk-ваш-openai-ключ
```

### Использовать Ollama (локально)

```kotlin
const val BASE_URL = "http://localhost:11434"
const val MODEL = "llama2"
// API ключ не требуется для локального Ollama
```

### Другие провайдеры

Любой OpenAI-совместимый провайдер работает!
- OpenRouter
- Together AI
- Groq
- LocalAI
- и другие...

## 💡 Модели DeepSeek

### deepseek-chat (рекомендуется)
- Универсальная модель для общения
- Быстрая и качественная
- Хорошо понимает русский язык

### deepseek-coder
- Специализированная модель для программирования
- Отлично генерирует и объясняет код
- Поддерживает множество языков программирования

Изменить модель:
```kotlin
// В ApiConfig.kt
const val MODEL = "deepseek-coder"  // Для программирования
```

## 🔒 Безопасность

### ❌ НЕ делайте:
- Не коммитьте `local.properties` в Git
- Не храните ключ в исходном коде
- Не делитесь ключом публично

### ✅ Делайте:
- Используйте `local.properties` для разработки
- Храните ключ в environment variables для CI/CD
- Используйте backend proxy для production
- Регулярно ротируйте ключи

## 📊 Мониторинг использования

Проверяйте использование API на:
https://platform.deepseek.com/usage

Там вы увидите:
- Количество запросов
- Использованные токены
- Стоимость
- История вызовов

## 🐛 Решение проблем

### Ошибка: "API ключ не настроен"

**Причина:** Ключ не установлен в `local.properties`

**Решение:**
1. Проверьте что файл `local.properties` существует
2. Убедитесь что ключ правильно скопирован
3. Перезапустите приложение

### Ошибка: "401 Unauthorized"

**Причина:** Неправильный API ключ

**Решение:**
1. Проверьте ключ на https://platform.deepseek.com/api_keys
2. Убедитесь что ключ активен
3. Создайте новый ключ если нужно

### Ошибка: "Rate limit exceeded"

**Причина:** Превышен лимит запросов

**Решение:**
1. Подождите 1 минуту
2. Проверьте лимиты на dashboard
3. Рассмотрите апгрейд плана

### Ошибка: "Insufficient balance"

**Причина:** Закончились средства на балансе

**Решение:**
Пополните баланс на https://platform.deepseek.com/

## 💰 Стоимость

DeepSeek значительно дешевле OpenAI:

| Провайдер | Модель | Цена (1M токенов) |
|-----------|--------|-------------------|
| DeepSeek | deepseek-chat | ~$0.14 |
| OpenAI | gpt-3.5-turbo | ~$0.50 |
| OpenAI | gpt-4 | ~$30.00 |

**Пример:** 1000 запросов ≈ $0.10-0.50 (в зависимости от длины)

## 📚 Дополнительные ресурсы

- **Официальная документация:** https://platform.deepseek.com/docs
- **API Reference:** https://platform.deepseek.com/api-docs
- **Pricing:** https://platform.deepseek.com/pricing
- **Community:** https://github.com/deepseek-ai

## 🎉 Готово!

Теперь ваше приложение использует DeepSeek API с безопасным хранением ключа!

**Следующие шаги:**
1. Протестируйте различные вопросы
2. Попробуйте модель `deepseek-coder` для программирования
3. Мониторьте usage на dashboard
4. Наслаждайтесь быстрыми и качественными ответами!

---
**Совет:** DeepSeek отлично работает на русском языке, так что смело задавайте вопросы по-русски! 🇷🇺